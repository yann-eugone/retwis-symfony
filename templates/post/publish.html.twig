{% extends 'base.html.twig' %}

{% block title 'Publish' %}

{% block body %}
    <h1 class="pb-3 mb-4 font-italic border-bottom">Publish post</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Publish</button>
    {{ form_end(form) }}
{% endblock %}

