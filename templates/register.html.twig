{% extends 'base.html.twig' %}

{% block title 'Register' %}

{% block body %}
    <h1 class="pb-3 mb-4 font-italic border-bottom">Please sign up</h1>
    <div class="row">
        <div class="col-md-6">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-lg btn-primary" type="submit">Register</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

