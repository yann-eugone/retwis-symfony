<h2>Recently registered users</h2>
<ul>
    {% for user in users %}
        <li>
            <a href="{{ path('user_profile', {'username': user.username}) }}">{{ user.username }}</a>
        </li>
    {% endfor %}
</ul>
