{# @var userFollower \App\User\User #}
{# @var userFollowing \App\User\User #}
{% extends 'base.html.twig' %}

{% block title user.username %}

{% block body %}
    <h1 class="pb-3 mb-4 font-italic border-bottom retwis-user-profile-name">
        {{ post.author.name }}<br>
        <small class="text-muted">@{{ user.username }}</small>
    </h1>
    <div class="row mb-4">
        <div class="offset-3 col-md-6">
            <h3 class="pb-3 mb-4 font-italic border-bottom">Fill profile</h3>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">Update profile</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
